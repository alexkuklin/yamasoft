apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helmexercise.fullname" . }}-test-connection"
  labels:
    {{- include "helmexercise.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helmexercise.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
